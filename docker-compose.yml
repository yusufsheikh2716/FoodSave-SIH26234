version: "3.9"

services:
  postgres:
    image: postgres:16-alpine
    container_name: foodsave-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: foodsave
      POSTGRES_PASSWORD: foodsave_dev
      POSTGRES_DB: foodsave
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U foodsave"]
      interval: 5s
      timeout: 3s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: foodsave-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redisdata:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

volumes:
  pgdata:
  redisdata:
